#include "kbalgorithms.h"

int fg_stats_sum_threshold_loop(kb_model_t *kb_model, int16_data_t *cols_to_use, float_data_t *params, FLOAT *pFV)
{
    int icol = 0;

    while (true)
    {
        icol += 1;
    }
    return cols_to_use->size;
}